name: Deploy React to AWS
on:
  push:
    branches:
      - 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2

      - name: Generate deployment package
        run: zip -r deploy.zip . -x '*.git*'

      - name: Deploy to EB
        uses: einaregilsson/beanstalk-deploy@v21
        with:
          aws_access_key: AKIA2ONANONDNIM5XNYB
          aws_secret_key: Hqq6oazs8ynhIq5WVyYvcGNCze4pBYxVKOcwtcIh
          application_name: react-sample-application-final
          environment_name: Reactsampleapplicationfinal-env
          version_label: ${{ github.sha }}
          region: ap-south-1
          deployment_package: deploy.zip
